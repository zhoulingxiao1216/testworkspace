# 🚀 Dify 全量迁移实施路线图 (Dify Migration Roadmap)

> **目标**：将依靠提示词驱动的 `kickoff_test_engine.md` 7大阶段，全量重构为 Dify 可视化工作流 / 聊天流（Chatflow）。
> **预估总耗时**：约 2 小时结对编程（Pair-Programming）。

---

## ⏳ 阶段一：资产结构化挂载（约 15 分钟）
**核心目标**：构建引擎的“不可篡改法典”（抗幻觉基础）。
* **人工动作**：在 Dify 的【知识库】中，创建名为“全局测试规约库”的实体。将本地 `00_全局测试规约库` 文件夹内的《测试点大纲编写规范》、《用例缩写字典》等核心规约文件上传。
* **架构支撑**：AI 协助配置分段（Chunk）大小与重叠度策略，优化向量检索精度（Top-K 等参数），确保后续大模型抽取规则时精准无误。

## ⏳ 阶段二：大脑切片与节点定义（约 1 小时）【最核心】
**核心目标**：将 7 阶段臃肿的提示词，按业务逻辑物理隔离为 4 个流转节点。
1. **代码探针节点 (Code Node)**：编写 Dify Python 预处理脚本，清洗用户输入的乱码 PRD，提取基础环境变量。
2. **风控前置大脑 (LLM Node A)**：
   * *职责*：审阅 PRD，对接知识库，输出项目的【Superpower 约束】及【模块前缀字典表】。
   * *配置*：提供专门的扁平化系统提示词。
3. **大纲骨架生成器 (LLM Node B)**：
   * *职责*：承接上文的 Superpower，只负责生成极简的思维导图式《测试点大纲》。
   * *配置*：提示词强制关闭“步骤生成”权限，杜绝幻觉。
4. **血肉穷举器 (LLM Node C)**：
   * *职责*：对着大纲，查阅全量 PRD，生成标准的 Markdown 5行数据用例表。

## ⏳ 阶段三：自动化红线布防设计（约 20 分钟）
**核心目标**：实现 CI/CD 流水线的强制风控隔离与输出清洗。
* **参数提取与校验**：配置 Dify 的结构化提取工具或后置代码节点，检查 LLM 输出是否包含非法格式（如无序列表）。如果不合规，触发 **Fail-Retry（失败重装）连线**。
* **P0/SmokeTest 锚定**：在流程中布设高危关键词检测（如"支付"、"权限"），自动为用例标题打上物理隔离标签 `[SmokeTest]`。

## ⏳ 阶段四：实战带水压测与调优（约 25 分钟）
**核心目标**：用真实长文本检验管线承压能力。
* **动作**：提取《会员体系优化PRD》等数千字真实业务文档作为入参。
* **联调**：观察画板各节点的流水灯运转状况。如发现某一环节漏填、错填，立马针对性进入该节点微调其 Prompt 和解析温度（Temperature）。

---

> **下周实施建议：**
> 下次开始干活时，直接把这份文件的第一阶段截图发在对话框里。我将直接为您输出对应阶段需要粘贴的所有 Prompt 代码块和参数值，咱们一个节点一个节点地拼装，2 个小时内一定可以宣告大盘点火！
