项目：消息提醒功能以及重要待办
run_id：message_reminder_20260525_001

任务说明（请阅读并执行）:
- 你是 Agent2（测试执行官）。先读取 memory：memory/shared/、memory/agent2-test-execution/。
- 先做环境预检（后台和前台），验证能否登录并完成基本页面流转；对每项检查记录状态与证据（截图/日志）。

测试环境（URL & 凭据引用，敏感信息请从本地安全文件读取）：
- 后台 URL： https://hlc-admin.hubbuyer.com/
  - 凭据引用：请从本地文件 `测试环境使用账号.md` 中读取条目 `hlc-admin`（不要把账号/密码写入 memory 或 reflections）。
- 前台 URL： https://hlc-b2b.hubbuyer.com/
  - 凭据引用：请从本地文件 `测试环境使用账号.md` 中读取条目 `hlc-b2b`（不要把账号/密码写入 memory 或 reflections）。

优先动作：
1) 环境能否登录（后台/前台）？请记录 是/否，并保存截图到 `.agent/runs/message_reminder_20260525_001/screens/`。
2) 若能登录，执行「创建/发送测试提醒→前台接收/展示」的关键路径并记录结果。
3) 若遇阻塞，立即产出 exchange 条目（`F-xxx`）并写入 `exchange/`，停止该用例后续执行，等待 Orchestrator 分类。

必须产物（结构化返回）：
1) `execution_log_path`：`.agent/runs/message_reminder_20260525_001/execution_log.jsonl`（每条记录包含 case_id/status/evidence_path/notes）
2) `exchange_items`：数组（每项为 `F-xxx` 格式，参见 `.agent/templates/exchange_item_template.json`）
3) `evidence_paths`：截图/日志路径列表（保存到 `.agent/runs/message_reminder_20260525_001/screens/`）
4) `reflections_path`：例如 `.agent/reflections/message_reminder_20260525_001_agent2_execution.md`（reflections 中不得包含明文密码/Token/原始私密数据；仅引用凭据来源）

敏感信息处理（必须遵守）：
- 绝对不得把密码/Token/账号写入任何 `memory/` 文件或 evolution/shared。仅允许引用本地凭据文件名或凭据 id（例如 `测试环境使用账号.md#hlc-admin`）。
- 证据截图如含个人信息，请先遮挡或模糊后再保存/上报。

示例返回格式（请按此结构返回 JSON）：
{
  "execution_log_path": ".agent/runs/message_reminder_20260525_001/execution_log.jsonl",
  "exchange_items": [ { "id":"F-001", "title":"缺少测试账号", "type":"external_blocker", "severity":"high", "evidence":[".agent/runs/message_reminder_20260525_001/screens/acc_missing.png"], "suggested_action":"提供 test account or mock seed" } ],
  "evidence_paths": [ ".agent/runs/message_reminder_20260525_001/screens/step1.png" ],
  "reflections_path": ".agent/reflections/message_reminder_20260525_001_agent2_execution.md"
}

请将上述消息整段复制并粘贴到 Agent2 的会话中开始工作。