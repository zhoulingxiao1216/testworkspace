# 订单详情页优化测试用例

生成日期：2026-05-22  
需求来源：`订单详情优化.docx`  
说明：PRD 标记“暂缓/开发评估”的需求已保留条件用例，执行前以 `审计待确认问题单.md` 的确认结果决定 Execute 或 Skip。

## 1. 订单详情页筛选栏

### 1.1 筛选栏展示与默认值

#### TC-OD-FLT-001 【正向】订单详情页筛选栏展示 [SmokeTest]

| 字段 | 内容 |
|:--|:--|
| **测试点** | 订单详情页新增商品级筛选栏，字段完整展示 |
| **优先级** | Critical |
| **前置条件** | 使用有权限账号登录全球站后台；存在进行中代购订单 |
| **操作步骤** | 1. 进入 B2B系统 → 订单管理 → 代购订单 → 进行中 <br> 2. 打开任一订单详情页 <br> 3. 查看商品列表上方或 UI 指定位置的筛选区域 |
| **预期结果** | 1. 筛选栏正常展示 <br> 2. 包含商品 ID、商品状态、采购状态、质检状态四类筛选项 <br> 3. 原有商品信息、采购信息、操作按钮正常展示 |

#### TC-OD-FLT-002 【正向】筛选项默认值和下拉选项完整

| 字段 | 内容 |
|:--|:--|
| **测试点** | 商品 ID 默认值、商品状态/采购状态/质检状态下拉选项 |
| **优先级** | High |
| **前置条件** | 已进入任一代购订单详情页 |
| **操作步骤** | 1. 查看商品 ID 输入框默认值 <br> 2. 展开商品状态下拉框 <br> 3. 展开采购状态下拉框 <br> 4. 展开质检状态下拉框 |
| **预期结果** | 1. 商品 ID 默认空 <br> 2. 商品状态默认全部，选项为全部、待入库、部分入库、已入库 <br> 3. 采购状态默认全部，选项为全部、待采购、部分到货、暂不购买 <br> 4. 质检状态默认全部，选项为全部、有不良、有待定、有退货、有换货、待检品 |

### 1.2 商品 ID 精确匹配

#### TC-OD-FLT-003 【正向】商品 ID 按 SKU 精确匹配 [SmokeTest]

| 字段 | 内容 |
|:--|:--|
| **测试点** | 商品 ID 精确匹配，不按部分字符模糊命中 |
| **优先级** | Critical |
| **前置条件** | 订单中存在 SKU 为 `SKU-A001` 的商品，同时存在相似 SKU 如 `SKU-A001-1` 或 `A001` |
| **操作步骤** | 1. 输入完整 SKU `SKU-A001` 查询 <br> 2. 改为输入部分 SKU `A001` 或 `SKU-A` 查询 |
| **预期结果** | 1. 输入完整 SKU 时仅展示精确匹配商品 <br> 2. 输入部分 SKU 时不应按模糊规则命中，除非产品另有确认 <br> 3. 筛选后商品数量、店铺归属、商品状态不串值 |

#### TC-OD-FLT-004 【异常】商品 ID 无结果空态和特殊字符安全

| 字段 | 内容 |
|:--|:--|
| **测试点** | 不存在 SKU、特殊字符 SKU、脚本字符输入的处理 |
| **优先级** | High |
| **前置条件** | 准备不存在 SKU；准备包含横杠、下划线、空格、大小写或多语言字符的 SKU |
| **操作步骤** | 1. 输入不存在 SKU 查询 <br> 2. 输入特殊字符 SKU 查询 <br> 3. 输入包含 HTML/脚本字符的内容查询 |
| **预期结果** | 1. 不存在 SKU 展示空结果提示，不白屏 <br> 2. 特殊字符 SKU 按精确匹配规则处理 <br> 3. 输入内容被安全处理，不触发脚本，不破坏布局 |

### 1.3 商品状态筛选

#### TC-OD-FLT-005 【正向】商品状态-待入库筛选 [SmokeTest]

| 字段 | 内容 |
|:--|:--|
| **测试点** | 待入库筛选规则 |
| **优先级** | Critical |
| **前置条件** | 存在正品数量更新后和已入库数量不同，且已入库数量为 0 的商品 |
| **操作步骤** | 1. 打开订单详情页 <br> 2. 商品状态选择“待入库”并查询 <br> 3. 对比商品正品数量和已入库数量 |
| **预期结果** | 1. 仅展示符合待入库规则的商品 <br> 2. 不符合规则的商品不展示 <br> 3. 商品数量、状态、店铺分组与实际数据一致 |

#### TC-OD-FLT-006 【正向】商品状态-部分入库筛选 [SmokeTest]

| 字段 | 内容 |
|:--|:--|
| **测试点** | 部分入库筛选规则 |
| **优先级** | Critical |
| **前置条件** | 存在正品数量和已入库数量不相同，且已入库数量不为 0 的商品 |
| **操作步骤** | 1. 商品状态选择“部分入库”并查询 <br> 2. 对比商品的正品数量和已入库数量 |
| **预期结果** | 1. 仅展示符合部分入库规则的商品 <br> 2. 已入库数量为 0 的商品不归入部分入库 <br> 3. 正品数量等于已入库数量的商品不归入部分入库 |

#### TC-OD-FLT-007 【正向】商品状态-已入库筛选 [SmokeTest]

| 字段 | 内容 |
|:--|:--|
| **测试点** | 已入库筛选规则 |
| **优先级** | Critical |
| **前置条件** | 存在正品数量和已入库数量相同，且发货数量为 0 的商品 |
| **操作步骤** | 1. 商品状态选择“已入库”并查询 <br> 2. 查看筛选结果中商品的正品数量、已入库数量、发货数量 |
| **预期结果** | 1. 仅展示正品数量=已入库数量且发货数量=0 的商品 <br> 2. 发货数量不为 0 的商品归属需按产品确认结果校验，不做确定推断 |

### 1.4 采购状态和质检状态筛选

#### TC-OD-FLT-008 【正向】采购状态筛选

| 字段 | 内容 |
|:--|:--|
| **测试点** | 待采购、部分到货、暂不购买筛选规则 |
| **优先级** | High |
| **前置条件** | 分别准备待采购、到货数小于采购数、通过“暂不购买”按钮产生状态的商品 |
| **操作步骤** | 1. 采购状态选择“待采购”查询 <br> 2. 采购状态选择“部分到货”查询 <br> 3. 采购状态选择“暂不购买”查询 |
| **预期结果** | 1. 待采购筛出允许采购审核完成后还未更新采购数据的商品 <br> 2. 部分到货筛出到货数小于采购数的商品 <br> 3. 暂不购买筛出通过暂不购买按钮产生对应状态的商品 |

#### TC-OD-FLT-009 【正向】质检状态筛选

| 字段 | 内容 |
|:--|:--|
| **测试点** | 有不良、有待定、有退货、有换货、待检品筛选 |
| **优先级** | High |
| **前置条件** | 分别准备不良、待定、退货、换货、待检品数量大于 0 的商品 |
| **操作步骤** | 1. 依次选择各质检状态查询 <br> 2. 对比对应数量 <br> 3. 查看同时存在多类质检数量商品的命中结果 |
| **预期结果** | 1. 各筛选项仅展示对应数量大于 0 的商品 <br> 2. 一个商品同时存在多类质检数量时，是否可被多个状态命中需按产品确认口径校验 <br> 3. 筛选结果不重复、不丢失 |

### 1.5 多条件组合与状态恢复

#### TC-OD-FLT-010 【正向】多条件组合筛选按交集生效 [SmokeTest]

| 字段 | 内容 |
|:--|:--|
| **测试点** | 商品 ID、商品状态、采购状态、质检状态组合筛选 |
| **优先级** | Critical |
| **前置条件** | 订单中存在多商品，且商品覆盖多种状态 |
| **操作步骤** | 1. 输入商品 ID 并选择商品状态 <br> 2. 继续选择采购状态和质检状态 <br> 3. 多次切换条件查询 |
| **预期结果** | 1. 多条件按交集过滤 <br> 2. 不符合任一条件的商品不展示 <br> 3. 切换条件后结果实时或点击查询后按产品设计更新 |

#### TC-OD-FLT-011 【正向】筛选清空、刷新和返回

| 字段 | 内容 |
|:--|:--|
| **测试点** | 筛选状态恢复和页面路由 |
| **优先级** | Medium |
| **前置条件** | 已在订单详情页执行过筛选 |
| **操作步骤** | 1. 清空商品 ID，并将下拉项切回全部 <br> 2. 刷新当前详情页 <br> 3. 返回列表后进入另一笔订单详情 |
| **预期结果** | 1. 清空后恢复展示符合“全部”条件的数据 <br> 2. 刷新后的筛选状态按产品设计保留或重置，但数据不能错误 <br> 3. 进入另一笔订单后不带入错误商品或错误筛选结果 |

#### TC-OD-FLT-012 【正向】多店铺大订单筛选准确性

| 字段 | 内容 |
|:--|:--|
| **测试点** | 多店铺、多商品订单筛选准确性 |
| **优先级** | High |
| **前置条件** | 准备包含多个店铺、较多商品且状态分布复杂的订单 |
| **操作步骤** | 1. 打开大订单详情页 <br> 2. 分别按商品状态、采购状态、质检状态筛选 <br> 3. 查看店铺分组、商品列表、滚动和展开收起效果 |
| **预期结果** | 1. 筛选结果中的商品归属店铺正确 <br> 2. 店铺不串、商品不重复、不丢失 <br> 3. 页面渲染和滚动可接受，不出现明显卡顿 |

## 2. 金额确认与采购差价

### 2.1 采购差价负数拦截

#### TC-OD-AMT-001 【异常】采购差价为负数时阻断采购确认 [SmokeTest]

| 字段 | 内容 |
|:--|:--|
| **测试点** | 采购差价负数不允许采购确认 |
| **优先级** | Critical |
| **前置条件** | 产品确认该“暂缓”需求进入本次版本；订单详情中存在采购差价为负数的商品或商铺 |
| **操作步骤** | 1. 进入订单详情页采购确认区域 <br> 2. 点击采购确认 |
| **预期结果** | 1. 系统弹窗提醒 <br> 2. 提醒文案为“采购差价不允许为负数，请核对后再确认！” <br> 3. 采购确认不成功，确认状态不变 |

#### TC-OD-AMT-002 【边界】采购差价负数边界金额

| 字段 | 内容 |
|:--|:--|
| **测试点** | 采购差价金额边界 |
| **优先级** | Critical |
| **前置条件** | 准备采购差价分别为 `-0.01`、负小数、0、正数的订单或商品 |
| **操作步骤** | 1. 分别点击采购确认 <br> 2. 观察弹窗和确认结果 |
| **预期结果** | 1. 任意负数采购差价均被阻断 <br> 2. 0 和正数不应因负数规则被误拦截 <br> 3. 金额精度和舍入不导致误判 |

#### TC-OD-AMT-003 【异常】负数拦截后数据不误更新

| 字段 | 内容 |
|:--|:--|
| **测试点** | 采购确认失败后的数据一致性 |
| **优先级** | Critical |
| **前置条件** | 采购差价为负数，且当前未采购确认 |
| **操作步骤** | 1. 点击采购确认触发拦截 <br> 2. 关闭弹窗 <br> 3. 刷新详情页和进行中列表 |
| **预期结果** | 1. 采购确认状态仍为未确认 <br> 2. 金额是否确认筛选结果不被错误改变 <br> 3. 不产生错误操作日志或账务记录 |

#### TC-OD-AMT-004 【安全】后端接口防绕过采购确认负数规则

| 字段 | 内容 |
|:--|:--|
| **测试点** | 采购确认后端校验 |
| **优先级** | Critical |
| **前置条件** | 可通过接口或抓包方式模拟采购确认请求；采购差价为负数 |
| **操作步骤** | 1. 绕过前端按钮限制直接提交采购确认接口 <br> 2. 查看接口响应和订单状态 |
| **预期结果** | 1. 后端拒绝负数采购差价确认 <br> 2. 返回明确错误信息 <br> 3. 数据库/页面状态不被更新 |

### 2.2 金额是否确认筛选

#### TC-OD-AMT-005 【正向】金额是否确认筛选字段展示 [SmokeTest]

| 字段 | 内容 |
|:--|:--|
| **测试点** | 进行中列表新增金额是否确认筛选 |
| **优先级** | Critical |
| **前置条件** | 使用有权限账号进入代购订单进行中列表 |
| **操作步骤** | 1. 打开 B2B系统 → 订单管理 → 代购订单 → 进行中 <br> 2. 查看筛选区域 <br> 3. 展开金额是否确认下拉框 |
| **预期结果** | 1. 筛选区展示“金额是否确认”字段 <br> 2. 默认值为全部 <br> 3. 选项为全部、已确认、未确认 |

#### TC-OD-AMT-006 【正向】金额是否确认-已确认筛选

| 字段 | 内容 |
|:--|:--|
| **测试点** | 已确认订单筛选 |
| **优先级** | High |
| **前置条件** | 存在所有商铺/商品均已点击采购确认的订单 |
| **操作步骤** | 1. 金额是否确认选择“已确认”并查询 <br> 2. 打开结果中的订单详情核对采购确认状态 |
| **预期结果** | 1. 已完成采购确认的订单展示在结果中 <br> 2. 订单详情中的商铺/商品采购确认状态与列表筛选一致 <br> 3. 未确认订单不应出现在已确认结果中 |

#### TC-OD-AMT-007 【正向】金额是否确认-未确认基础规则 [SmokeTest]

| 字段 | 内容 |
|:--|:--|
| **测试点** | 未确认订单筛选基础规则 |
| **优先级** | Critical |
| **前置条件** | 存在包含“采购确认”按钮未点击确认商铺的订单 |
| **操作步骤** | 1. 金额是否确认选择“未确认”并查询 <br> 2. 打开筛选结果中的订单详情核对 |
| **预期结果** | 1. 存在未点击采购确认商铺的订单可被筛出 <br> 2. 详情页对应商铺/商品仍处于未采购确认状态 |

#### TC-OD-AMT-008 【正向】多商铺订单任一未确认归入未确认 [SmokeTest]

| 字段 | 内容 |
|:--|:--|
| **测试点** | 多商铺订单金额确认状态 |
| **优先级** | Critical |
| **前置条件** | 准备多商铺订单，其中至少一个商品或商铺未采购确认，其余已确认 |
| **操作步骤** | 1. 金额是否确认选择“未确认”查询 <br> 2. 查看该多商铺订单是否出现在结果中 <br> 3. 打开详情核对各商铺状态 |
| **预期结果** | 1. 只要有一个商品或商铺未采购确认，该订单归入未确认 <br> 2. 详情页未确认位置准确，不影响已确认商铺状态 |

#### TC-OD-AMT-009 【正向】未确认筛选额外条件满足

| 字段 | 内容 |
|:--|:--|
| **测试点** | 未确认筛选需同时满足数量公式 |
| **优先级** | Critical |
| **前置条件** | 存在金额未确认且采购数=不良数+正品数的订单 |
| **操作步骤** | 1. 金额是否确认选择“未确认”查询 <br> 2. 核对结果订单的采购数、不良数、正品数 |
| **预期结果** | 1. 同时满足金额未确认和采购数=不良数+正品数的订单展示 <br> 2. 数量口径与详情页一致 |

#### TC-OD-AMT-010 【反向】未确认筛选额外条件不满足

| 字段 | 内容 |
|:--|:--|
| **测试点** | 未确认筛选数量公式不满足时不展示 |
| **优先级** | Critical |
| **前置条件** | 存在金额未确认但采购数≠不良数+正品数的订单 |
| **操作步骤** | 1. 金额是否确认选择“未确认”查询 <br> 2. 搜索或翻页查看该订单是否出现 |
| **预期结果** | 1. 该订单不展示在未确认筛选结果中 <br> 2. 两个条件任一不满足均不展示 |

## 3. 店铺返现二次确认

### 3.1 返现确认展示与金额口径

#### TC-OD-CB-001 【正向】店铺返现二次确认按钮展示

| 字段 | 内容 |
|:--|:--|
| **测试点** | 店铺返现新增二次确认按钮 |
| **优先级** | Critical |
| **前置条件** | 产品确认该“开发评估”需求进入本次版本；订单详情中存在店铺返现字段 |
| **操作步骤** | 1. 进入订单详情页采购相关区域 <br> 2. 查看店铺返现字段和确认按钮 <br> 3. 分别查看未确认、已确认状态 |
| **预期结果** | 1. 店铺返现字段旁有二次确认按钮 <br> 2. 未确认按钮为红色 <br> 3. 已确认按钮为绿色 <br> 4. 按钮文案和具体样式以最终 UI 图为准 |

#### TC-OD-CB-002 【正向】未确认店铺返现不影响采购差价 [SmokeTest]

| 字段 | 内容 |
|:--|:--|
| **测试点** | 未确认前店铺返现不进入采购差价 |
| **优先级** | Critical |
| **前置条件** | 店铺返现处于未确认状态 |
| **操作步骤** | 1. 在店铺返现字段输入金额 <br> 2. 保存但不点击二次确认 <br> 3. 查看采购差价金额 |
| **预期结果** | 1. 输入金额只展示/保存于店铺返现栏 <br> 2. 采购差价不因未确认返现金额而变化 <br> 3. 页面刷新后未确认返现金额仍可查看 |

#### TC-OD-CB-003 【正向】店铺返现二次确认后同步和锁定 [SmokeTest]

| 字段 | 内容 |
|:--|:--|
| **测试点** | 店铺返现确认后的计算和锁定 |
| **优先级** | Critical |
| **前置条件** | 店铺返现有未确认金额；采购员账号登录 |
| **操作步骤** | 1. 点击店铺返现二次确认按钮 <br> 2. 按页面提示完成确认 <br> 3. 查看采购差价、按钮状态和字段可编辑性 |
| **预期结果** | 1. 店铺返现状态变为已确认，按钮变为绿色 <br> 2. 店铺返现金额是否同步到采购差价，按产品最终确认规则校验 <br> 3. 确认后采购员无权限再次修改店铺返现金额 |

#### TC-OD-CB-004 【权限】店铺返现确认后角色权限

| 字段 | 内容 |
|:--|:--|
| **测试点** | 采购员、主管/管理员对已确认店铺返现的修改权限 |
| **优先级** | High |
| **前置条件** | 店铺返现已确认；准备采购员、主管或管理员账号 |
| **操作步骤** | 1. 采购员尝试修改已确认店铺返现金额 <br> 2. 主管或管理员尝试修改或反确认 <br> 3. 查看页面提示和数据保存结果 |
| **预期结果** | 1. 采购员不可再次修改 <br> 2. 主管或管理员是否可修改或反确认，按产品确认结果执行 <br> 3. 不允许越权接口绕过 |

#### TC-OD-CB-005 【边界】店铺返现金额输入边界

| 字段 | 内容 |
|:--|:--|
| **测试点** | 店铺返现金额输入校验 |
| **优先级** | High |
| **前置条件** | 店铺返现可编辑且未确认 |
| **操作步骤** | 1. 输入空值、0、正数、小数、负数、超大金额、非数字字符 <br> 2. 保存或确认 <br> 3. 查看店铺返现和采购差价 |
| **预期结果** | 1. 合法金额可按规则保存 <br> 2. 非法金额有明确提示 <br> 3. 金额精度和采购差价影响符合产品确认口径 |

## 4. 进行中列表筛选排序

### 4.1 金额确认筛选联动

#### TC-OD-LST-001 【正向】金额是否确认筛选与分页/其他条件联动

| 字段 | 内容 |
|:--|:--|
| **测试点** | 金额是否确认筛选与其他查询条件、分页、刷新联动 |
| **优先级** | High |
| **前置条件** | 列表存在多页数据；有其他可用筛选条件 |
| **操作步骤** | 1. 选择金额是否确认为未确认 <br> 2. 叠加订单号、时间或其他列表筛选条件 <br> 3. 翻页、刷新、重置条件 |
| **预期结果** | 1. 多筛选条件按交集查询 <br> 2. 分页总数和列表结果正确 <br> 3. 刷新和重置后筛选状态符合产品设计 |

### 4.2 购入数排序

#### TC-OD-LST-002 【正向】购入数排序按钮展示

| 字段 | 内容 |
|:--|:--|
| **测试点** | 购入数字段新增排序按钮 |
| **优先级** | High |
| **前置条件** | 产品确认该“开发评估”需求进入本次版本；进入代购订单进行中列表 |
| **操作步骤** | 1. 查看列表“购入数”字段 <br> 2. 查看是否存在排序按钮或排序图标 |
| **预期结果** | 1. 购入数字段展示排序入口 <br> 2. 排序入口样式和交互以最终 UI 为准 |

#### TC-OD-LST-003 【正向】购入数量为空订单置顶 [SmokeTest]

| 字段 | 内容 |
|:--|:--|
| **测试点** | 点击购入数排序后空购入数置顶 |
| **优先级** | Critical |
| **前置条件** | 列表中同时存在购入数量为空和购入数量有值的订单 |
| **操作步骤** | 1. 点击购入数排序按钮 <br> 2. 查看排序后的列表顺序 |
| **预期结果** | 1. 购入数量为空的订单自动排到最上面 <br> 2. 非空购入数订单排在空值订单之后 <br> 3. 空值订单之间的次级排序按产品确认规则校验 |

#### TC-OD-LST-004 【边界】购入数 0 与空值排序边界

| 字段 | 内容 |
|:--|:--|
| **测试点** | 购入数 0 是否等同为空 |
| **优先级** | High |
| **前置条件** | 列表中存在购入数为空、购入数为 0、购入数大于 0 的订单 |
| **操作步骤** | 1. 点击购入数排序 <br> 2. 观察三类订单排序位置 |
| **预期结果** | 1. 购入数为空订单置顶 <br> 2. 购入数为 0 是否等同为空，按产品确认结果校验 <br> 3. 不应将空字符串、null、0 混淆导致排序异常 |

#### TC-OD-LST-005 【正向】购入数排序与金额确认筛选联动

| 字段 | 内容 |
|:--|:--|
| **测试点** | 排序与筛选组合 |
| **优先级** | High |
| **前置条件** | 存在满足金额未确认筛选的数据，且其中包含购入数为空和非空订单 |
| **操作步骤** | 1. 金额是否确认选择“未确认”并查询 <br> 2. 点击购入数排序 <br> 3. 翻页查看结果 |
| **预期结果** | 1. 排序只作用于当前筛选结果集 <br> 2. 空购入数订单在筛选结果内置顶 <br> 3. 分页总数和排序结果一致 |

#### TC-OD-LST-006 【正向】购入数排序全量分页校验

| 字段 | 内容 |
|:--|:--|
| **测试点** | 排序范围覆盖全量结果 |
| **优先级** | High |
| **前置条件** | 进行中列表存在多页数据，第二页或更后页面存在购入数为空订单 |
| **操作步骤** | 1. 在第一页点击购入数排序 <br> 2. 查看排序后第一页数据 <br> 3. 翻到后续页核对 |
| **预期结果** | 1. 排序基于全量查询结果 <br> 2. 后续页的空购入数订单应被提前到排序后的前部结果中 <br> 3. 不出现仅当前页局部排序的问题 |

#### TC-OD-LST-007 【正向】购入数排序恢复默认排序

| 字段 | 内容 |
|:--|:--|
| **测试点** | 取消排序或切换排序后的状态恢复 |
| **优先级** | Medium |
| **前置条件** | 购入数排序已生效 |
| **操作步骤** | 1. 再次点击排序按钮或点击重置 <br> 2. 切换其他筛选条件后返回默认列表 |
| **预期结果** | 1. 列表恢复产品约定的默认排序 <br> 2. 排序状态展示与实际结果一致 <br> 3. 不残留错误排序条件 |

## 5. 权限安全与通用回归

### 5.1 权限、异常与审计

#### TC-OD-SEC-001 【权限】不同角色权限控制 [SmokeTest]

| 字段 | 内容 |
|:--|:--|
| **测试点** | 不同角色访问订单详情筛选、采购确认、店铺返现确认、列表筛选排序权限 |
| **优先级** | Critical |
| **前置条件** | 准备采购员、无权限账号、管理员或主管账号 |
| **操作步骤** | 1. 不同角色访问订单详情筛选栏 <br> 2. 不同角色执行采购确认、店铺返现确认或修改 <br> 3. 不同角色访问进行中列表筛选和排序 |
| **预期结果** | 1. 有权限角色可见可操作 <br> 2. 无权限角色不可查看或操作敏感功能 <br> 3. 接口层不可越权 |

#### TC-OD-SEC-002 【异常】接口失败和加载态

| 字段 | 内容 |
|:--|:--|
| **测试点** | 筛选、排序、确认接口异常 |
| **优先级** | High |
| **前置条件** | 可模拟接口超时、失败或返回空数据 |
| **操作步骤** | 1. 执行详情页筛选 <br> 2. 执行列表金额确认筛选或购入数排序 <br> 3. 执行采购确认或店铺返现确认 <br> 4. 分别模拟接口失败 |
| **预期结果** | 1. 页面有 loading、错误提示或重试机制 <br> 2. 接口失败不导致整页白屏 <br> 3. 失败后数据不被误更新 |

#### TC-OD-SEC-003 【兼容】页面兼容性和布局

| 字段 | 内容 |
|:--|:--|
| **测试点** | 筛选栏、弹窗、列表在不同分辨率下展示 |
| **优先级** | Medium |
| **前置条件** | 可调整浏览器窗口和缩放比例 |
| **操作步骤** | 1. 在 1366、1440、1920 宽度查看订单详情和进行中列表 <br> 2. 浏览器缩放 90%、100%、125% <br> 3. 打开下拉框、弹窗并执行筛选 |
| **预期结果** | 1. 筛选栏不遮挡、不换行错乱 <br> 2. 下拉框和弹窗位置正确 <br> 3. 列表字段、排序按钮、操作按钮可见可点 |

#### TC-OD-SEC-004 【安全】输入内容 XSS 与布局安全

| 字段 | 内容 |
|:--|:--|
| **测试点** | SKU、商品名、备注等内容包含特殊字符时页面安全展示 |
| **优先级** | High |
| **前置条件** | 准备包含 HTML 标签、脚本字符、多语言字符、超长字符的商品或 SKU |
| **操作步骤** | 1. 打开订单详情页 <br> 2. 执行商品 ID 筛选 <br> 3. 查看商品列表、筛选空态、弹窗和列表字段 |
| **预期结果** | 1. 特殊字符被安全转义 <br> 2. 不触发脚本执行 <br> 3. 页面布局不被破坏 |

#### TC-OD-SEC-005 【审计】关键操作日志和审计追溯

| 字段 | 内容 |
|:--|:--|
| **测试点** | 采购确认、店铺返现确认等关键动作的记录 |
| **优先级** | Medium |
| **前置条件** | 系统支持订单日志、操作日志或后台变更记录 |
| **操作步骤** | 1. 执行采购确认 <br> 2. 执行店铺返现二次确认 <br> 3. 查看订单日志、操作记录或相关后台记录 |
| **预期结果** | 1. 成功操作有操作人、时间、操作内容记录 <br> 2. 被负数采购差价拦截的操作不产生成功确认记录 <br> 3. 日志内容与实际操作一致 |

## 6. 待确认项对应用例

| 待确认项 | 关联用例 | 执行策略 |
|:--|:--|:--|
| 采购差价负数拦截是否进入本次版本 | TC-OD-AMT-001 至 TC-OD-AMT-004 | 未确认前标记 Skip-待版本确认 |
| 店铺返现二次确认是否进入本次版本 | TC-OD-CB-001 至 TC-OD-CB-005 | 未确认前标记 Skip-待版本确认 |
| 购入数排序是否进入本次版本 | TC-OD-LST-002 至 TC-OD-LST-007 | 未确认前标记 Skip-待版本确认 |
| 商品 ID 字段口径 | TC-OD-FLT-003、TC-OD-FLT-004 | 执行前确认 SKU 字段 |
| 多商品未确认数量公式口径 | TC-OD-AMT-009、TC-OD-AMT-010 | 执行前确认按任一商品、命中商品或全商品判断 |

