# Workspace Notes

## 目录约定

- 工作区根目录：`D:\test_workspace`
- 全局测试规约库：`D:\test_workspace\00_全局测试规约库`
- 业务测试项目根目录：`D:\test_workspace\测试项目`
- 测试平台目录：`D:\test_workspace\测试平台`

处理业务项目时，默认先在 `D:\test_workspace\测试项目\{项目名}` 下定位项目资料；只有该目录不存在，或用户明确给出其他路径时，才回退到工作区根目录下同名目录或用户指定路径。

## 测试平台启动约定

当用户说“启动测试平台”时，直接在 `D:\test_workspace\测试平台` 目录执行：

```powershell
streamlit run app.py
```

通常访问地址为：

```text
http://localhost:8501
```

如果 8501 已被占用，按 Streamlit 输出的实际端口告知用户。

## 测试报告生成约定

当用户要求“生成测试报告”“编写验收报告”“输出上线验收测试报告”或类似任务时，默认按 Agent1 上线验收测试报告规范执行，不需要用户重复说明格式。

执行顺序：

1. 先读取 `D:\test_workspace\00_全局测试规约库\执行规范\上线验收测试报告规范.md`。
2. 再参考同类已完成报告的结构和措辞，优先样本包括：
   - `D:\test_workspace\测试项目\表单下载\测试文档\上线验收测试报告_2026-05-20.md`
   - `D:\test_workspace\测试项目\国内包裹签收处理功能\测试文档\上线验收测试报告_2026-05-20.md`
   - `D:\test_workspace\测试项目\全球站批量导入功能\测试文档\上线验收测试报告_2026-05-20.md`
   - `D:\test_workspace\测试项目\消息提醒功能以及重要待办\测试文档\上线验收测试报告_2026-06-12.md`
3. 项目内优先读取 `测试文档\superpowers.md`、`测试点大纲.md`、`工期评估.md`、`执行交接清单.yaml`、最新 `special_*测试用例_*.xlsx`、最新 `协润进出口_缺陷_*.rtf`、Agent2 执行报告或缺陷清单。
4. 报告必须输出到对应项目的 `测试文档\上线验收测试报告_YYYY-MM-DD.md`；若项目位于 `测试项目` 下，输出路径也必须保留在 `D:\test_workspace\测试项目\{项目名}\测试文档\`。
5. Markdown 固定包含：报告摘要、项目与工期概况、测试覆盖与执行量化、用例执行结果统计、测试对象与文件覆盖、缺陷闭环、缺陷明细、Superpowers 核心规约验收审计、已知问题与遗留风险、上线注意事项、变更记录。
6. 发布结论必须按规范判定：全部有效用例通过且无核心风险才写“测试通过”；存在 Medium/Low 非阻塞遗留项可写“附条件上线”；存在 P0/P1 阻塞、核心链路失败、数据安全或权限边界未通过时写“暂缓上线”，不能弱化为普通通过。

## 文件与安全边界

- 不要把 `测试平台\.streamlit\secrets.toml`、登录凭据、Token、Cookie、企业微信 Webhook 明文写入报告、memory 或可提交文件。
- `测试平台\test_mission.db` 是测试平台本地数据文件，修改前必须明确用户意图；常规报告生成任务不应改动它。
- `output\`、脚本生成的 `reports\`、`token\`、`__pycache__\` 和日志目录只作为运行产物读取，非用户要求不要整理、移动或提交。
