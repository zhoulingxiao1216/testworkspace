# 审计待确认问题单

> 当前问题不阻塞 Agent1 产物生成，但会影响 Agent2 执行数据准备、联调断言和缺陷判定。

| 编号 | 问题 | 影响模块 | 风险等级 | 建议确认对象 |
|:--|:--|:--|:--|:--|
| Q-001 | `open.hubbuyer.com` 是否已完成 HTTPS 和反向代理配置；若临时走 `api.hubbuyer.com`，对外文档最终域名以哪个为准。 | AUTH / SRCH / DET / STAT | High | 开发 / 运维 |
| Q-002 | 1688 关键词搜索实际字段名、筛选字段、排序值是否与 PRD 推断完全一致。 | SRCH | High | 后端 / 1688 联调负责人 |
| Q-003 | 1688 商品详情接口实际入参是 `offerId` 还是 `offerIds`，以及 SKU、服务标签、物流字段的真实返回结构。 | DET | High | 后端 / 1688 联调负责人 |
| Q-004 | `JANコード` 的来源表、索引和匹配规则是否已确定；是否允许模糊匹配。 | STAT / UI | High | 后端 / 数据库 |
| Q-005 | 6 个对外日文状态与 `b2b_order.status`、`b2b_order_detail.order_mark` 的代码级映射表是否已出。 | STAT / UI | Critical | 后端 / 产品 |
| Q-006 | `limit > 100` 时是截断为 100 还是返回 400；PRD 推荐截断但保留两种实现口径。 | STAT | Medium | 后端 / 产品 |
| Q-007 | 生成新 Token 后旧 Token 是否立即失效，是否存在客户切换宽限期。 | AUTH / ADM | Critical | 后端 / 产品 |
| Q-008 | Client Secret 在后台列表是否完整展示或脱敏展示；复制行为是否需要二次确认或权限限制。 | ADM / LOG | Critical | 产品 / 安全 |
| Q-009 | 商品状态查询在导入记录存在但购物车/订单链路缺失时，`match_mode` 的枚举值和前台是否展示。 | STAT / UI | Medium | 后端 / 产品 |
| Q-010 | 前台商品数据查询页面认证是否直接换取 Bearer Token，还是只做后端 session 级认证。 | UI / AUTH | High | 前端 / 后端 |

