# 全球站项目：商品问题工单处理 PRD

## 1. 项目背景与目标

### 1.1 项目背景

当前代购订单在质检、采购、业务、客户确认之间存在大量商品问题确认场景，例如商品轻微瑕疵、色差、尺寸异常、供应商缺货、是否可继续发货等。原有处理方式依赖订单备注、口头沟通或分散记录，存在以下问题：

- 商品问题与订单商品行绑定不清晰，员工无法快速判断哪个订单、哪个商品需要处理。
- 发起问题后没有统一待办入口，处理人容易漏看、延迟处理或重复确认。
- 客户为海外客户时，需要业务翻译并对客沟通，但内部处理人与对客沟通人边界不清。
- 同一个商品可能产生多个问题工单，历史意见、客户回复和流转记录混杂，后续处理人阅读成本高。
- 工单关闭、完结、同步客户信息等动作缺少权限约束，容易出现未同步、误关闭或状态断层。

### 1.2 优化目标

1. 建立“订单商品行内发起、订单内处理、待办集中提醒”的商品问题工单闭环。
2. 简化流程：创建工单时只选择一个处理人；处理人可提交意见、转交或关闭；只有业务可发送客户确认；发起人负责最终完结。
3. 保证信息清晰：问题内容、处理意见、客户沟通、流转记录按类别展示，历史记录按时间倒序查看。
4. 提升操作顺畅度：订单详情可直接弹窗处理工单，待办页面也可进入同一处理弹窗，避免员工在页面之间反复跳转。
5. 统一模板能力：支持工单模板、多语言内容维护，创建工单时模板只回填中文，业务发送客户时再使用客户语言内容。
6. 支持全量追溯：有权限人员可在“历史记录”模式查看所有订单工单历史，不再仅限本人创建或本人处理。
7. 支持同规格核对：同一商品规格即使商品ID不同，也可快速查看不同订单下的历史工单，辅助判断当前问题是否需要处理。

---

## 2. 【管理端】订单内工单处理与协同待办

### 2.0 代购订单详情页

**① 核心业务逻辑介绍**

代购订单详情是商品问题工单的主要业务入口。员工在商品行中发现问题后，可直接创建工单；商品已有工单时，可点击工单处理按钮进入工单详情弹窗。该页面不承载完整工单处理表单，只负责展示当前商品是否存在工单、未完结数量、当前状态和快速入口。

**② 字段级别功能描述与异常流程**

**(1) 商品行工单入口区**

| 字段/控件名称 | 功能展现描述 | 业务规则与异常边界处理 |
| --- | --- | --- |
| 创建工单 / 工单处理 | 商品无工单时显示“创建工单”；商品存在工单时显示“工单处理”。按钮与状态角标合并成一个整体控件，避免操作区割裂。 | 点击“创建工单”打开创建工单弹窗；点击“工单处理”打开工单详情弹窗并默认选中当前商品最新未完结工单。无权限时按钮置灰并提示无处理权限。 |
| 工单数量角标 | 展示当前商品未完结工单数量，红色气泡显示在工单处理按钮内。 | 数量只统计未完结工单；工单完结后从数量中剔除。统计失败时不影响订单商品行展示，只隐藏角标。 |
| 工单状态标签 | 在按钮下方展示当前商品最新工单状态，例如待处理、待客户确认、客户已回复、客户已确认、已完结。 | 同一商品多工单时，以最新未完结工单作为状态摘要；全部完结时显示“暂无工单”或仅保留入口按钮。 |
| 内部备注 | 保留订单商品原有内部备注入口。 | 与工单记录相互独立，内部备注不自动生成工单。 |
| 特殊物品 / 黑名单 / 吐槽 | 内部备注下方展示三个图标，分别代表特殊物品、黑名单、吐槽。 | 仅作为商品辅助标识与快捷查看入口，不参与工单状态计算；图标无数据时可置灰。 |

**(2) 工单编号与商品绑定**

| 字段/控件名称 | 功能展现描述 | 业务规则与异常边界处理 |
| --- | --- | --- |
| 工单编号 | 工单编号只展示当前商品下的序号，例如 001、002、003。 | 序号按商品维度递增，同一商品不可重复；不同商品可各自从 001 开始。 |
| 关联商品 | 工单必须绑定订单商品行，后续处理均围绕该商品展开。 | 商品从订单中删除、拆分或合并时，工单仍保留原关联商品快照，避免历史记录丢失。 |
| 创建时间 | 工单创建时间在工单详情与我的待办中展示。 | 订单详情商品行不展示创建时间，避免操作区拥挤。 |

**③ 交互与跳转说明**

- 在订单详情点击“创建工单”后，页面以居中弹窗方式打开创建工单，不跳转页面。
- 在订单详情点击“工单处理”后，页面以居中弹窗方式打开工单详情，并保留当前订单上下文。
- 在商品没有工单时，首次创建成功后，商品行按钮从“创建工单”切换为“工单处理”，并显示红色数量角标。
- 在待客户确认阶段，订单商品行仍可查看工单详情，但不可转交该工单。

**④ 关联的隐藏后端模块及解决方案**

| 关联模块 | 影响点 | 解决方案 |
| --- | --- | --- |
| 订单商品行模块 | 工单数量、状态与订单商品强绑定。 | 工单主表保存订单号、商品ID、商品行ID和创建时商品快照；订单商品行查询时聚合未完结工单数量与最新状态。 |
| 工单编号服务 | 工单编号改为商品维度序号。 | 按商品ID生成递增序号，展示时只返回序号文本；后端仍保留内部唯一ID。 |
| 权限模块 | 不同角色在订单详情可见入口不同。 | 前端隐藏不可用动作，后端按当前登录人与工单当前处理人、发起人、业务角色二次校验。 |

#### 2.0.1 创建工单弹窗

**① 核心业务逻辑介绍**

创建工单弹窗用于从订单商品行发起一个新的商品问题工单。创建时只能选择一个处理人，避免多人并行处理导致责任不清。问题内容使用纯文本加图片附件，不再使用富文本；图片附件分为实物图和链接图，均支持点击上传、拖拽上传、复制粘贴上传、预览大图和删除。

**② 字段级别功能描述与异常流程**

**(1) 关联商品区**

| 字段/控件名称 | 功能展现描述 | 业务规则与异常边界处理 |
| --- | --- | --- |
| 商品图 | 展示当前商品缩略图。 | 图片加载失败时显示占位图，不阻断创建工单。 |
| 商品名称 | 展示商品名称。 | 名称过长时两行内展示，超出省略。 |
| 订单 | 展示订单编号。 | 点击订单号不在弹窗内跳转，避免创建流程中断。 |
| 商品ID | 展示商品ID，例如 D000005171。 | 商品ID为工单与商品绑定的关键字段，不使用蓝色强调，和其他字段统一展示。 |
| 规格 | 合并展示颜色与尺码，例如白色 / 3件套。 | 颜色或尺码为空时展示“--”，不得影响工单创建。 |
| MySKU / ASIN | 展示商品外部标识。 | 缺失时展示“--”。 |
| 国家 / 业务 / 采购 | 展示订单客户国家、业务员、采购人员。 | 作为处理人选择与对客语言判断参考，不允许在弹窗中编辑。 |
| 历史工单 | 展示当前商品规格在其他订单中的历史工单摘要，并支持按客户ID、订单编号、问题类型筛选。 | 按商品名称、规格、颜色、尺码、属性等标准化后匹配；商品ID不同也可命中。筛选仅在同规格结果内二次收窄，问题类型对应工单模板或自定义内容。创建工单弹窗内显示订单编号、客户编号、工单模板、创建时间和摘要，不展示状态；无历史工单时不展示该模块。 |

**(2) 工单填写区**

| 字段/控件名称 | 功能展现描述 | 业务规则与异常边界处理 |
| --- | --- | --- |
| 处理人 | 单选下拉框，只展示部门和人名，例如“采购 包瑞楠”“订单业务 唐芬芬”。 | 必填。创建后工单进入该处理人的待办；后面的岗位说明不在下拉选项中展示，避免干扰识别。 |
| 工单模板 | 下拉选择“自定义内容”“色差问题”“尺寸问题”等启用模板。 | 默认“自定义内容”。选择模板后只回填中文模板内容；即使处理人是业务，也不得在创建阶段回填英文。 |
| 问题内容 | 文本输入框，填写本次商品问题、处理诉求、需要确认内容。 | 必填。输入和展示都必须保留换行，避免长文本挤成一段。 |
| 实物图 | 上传质检、仓库拍摄的商品实物照片。 | 非必填。支持点击、拖拽、粘贴上传；上传失败时保留已输入文字并提示重试。 |
| 链接图 | 上传商品链接、商家页面、采购链接中的参考图片。 | 非必填。与实物图独立存储和展示；不得混在同一组导致来源不清。 |

**③ 交互与跳转说明**

- 选择工单模板后，问题内容自动替换为模板中文内容；用户可继续手动编辑。
- 点击历史工单时，必须打开新的浏览器 Tab 页展示对应订单/工单详情，不关闭、不覆盖当前创建弹窗，方便先核对历史处理结果再决定是否继续创建。
- 图片上传区域为一个整体组件，上传按钮与已上传图片同区展示；图片只展示缩略图，不额外显示“商品图”等默认文字。
- 点击“提交并进入处理人待办”后，弹窗关闭，订单商品行工单按钮和角标立即刷新。
- 提交失败时保持弹窗内容不清空，并在底部红色提示失败原因。

**④ 关联的隐藏后端模块及解决方案**

| 关联模块 | 影响点 | 解决方案 |
| --- | --- | --- |
| 工单主表 | 创建工单需要保存发起人、处理人、状态、关联商品快照、问题内容。 | 初始状态为“待处理”，当前处理人为所选处理人，发起人为当前登录人。 |
| 附件服务 | 图片来源分为实物图和链接图。 | 附件表保存附件类型、排序、预览地址、原始文件信息；支持后续按类型展示。 |
| 模板服务 | 模板回填中文内容，业务发送客户时再使用客户语言。 | 创建工单只读取中文模板；客户语言内容在业务处理阶段读取或由业务编辑确认。 |
| 历史工单检索服务 | 创建工单时需要快速定位历史工单。 | 保存商品规格标准化字段，按规格Key跨订单查询历史工单；支持客户ID、订单编号、问题类型作为结果筛选条件；商品ID不同但规格一致时也返回。 |
| 人员权限服务 | 创建工单处理人选择需要按部门角色过滤。 | 质控部门只展示初检、组长、主管，其他质控角色不进入处理人下拉；该项为PRD规则，页面原型不单独新增说明模块。 |

#### 2.0.2 工单详情弹窗

**① 核心业务逻辑介绍**

工单详情弹窗是工单处理的核心页面。页面分为三块：左侧当前商品工单列表，中间当前处理区，右侧历史记录区。员工处理工单时主要关注中间“当前处理”；历史信息按类别在右侧查看，减少从头翻阅所有记录的负担。

**② 字段级别功能描述与异常流程**

**(1) 顶部关联商品信息区**

| 字段/控件名称 | 功能展现描述 | 业务规则与异常边界处理 |
| --- | --- | --- |
| 弹窗标题 | 只展示“工单详情”。 | 不再展示“商品问题工单”等副标题，保持页面简洁。 |
| 商品信息 | 展示商品图、商品名称、订单、商品ID、规格、客户、国家、提出人、创建时间、当前处理人。 | 字段重新排为两行展示；规格合并颜色和尺码。字段缺失时展示“--”。 |
| 关闭图标 | 右上角仅保留 X 关闭弹窗。 | 关闭弹窗不改变工单状态。 |

**(2) 当前商品工单列表区**

| 字段/控件名称 | 功能展现描述 | 业务规则与异常边界处理 |
| --- | --- | --- |
| 当前商品工单 | 标题后显示红色数量气泡，统计当前商品工单数。 | 数量包含当前商品全部工单，已完结工单也保留在列表中用于查看。 |
| 新增工单 | 按钮文案为“新增工单”。 | 点击打开创建工单弹窗并带入当前商品。 |
| 工单卡片 | 展示工单编号、状态、创建时间、处理人。 | 卡片高度紧凑，编号过长时按序号展示；默认不高亮第一条，只有被选中时展示选中样式。 |
| 正在查看 | 当前选中工单按钮显示“正在查看”，按钮颜色采用选中态。 | 切换工单后，中间当前处理区和右侧历史记录同步切换。 |

**(3) 当前处理区**

| 状态 | 当前处理展示 | 可操作动作 | 业务规则与异常边界处理 |
| --- | --- | --- | --- |
| 待处理-非业务处理人 | 展示“本次处理意见”和图片附件。 | 保存草稿、提交意见、转交、关闭工单、确认完结。 | “提交意见”将意见同步给业务处理；图片非必填。只有发起人可看到“确认完结”；其他人只能处理、转交或关闭。 |
| 待处理-业务处理人 | 展示“发送客户内容”，内容默认可根据模板填充客户语言；图片分实物图和链接图。 | 保存草稿、发送客户确认、转交、关闭工单、确认完结。 | 业务可发送客户确认，也可转交其他处理人；发送客户时文字必填，图片非必填。 |
| 待客户确认 | 展示等待客户确认内容，包括问题内容、实物图、链接图。 | 关闭工单、确认完结。 | 客户确认阶段不可转交；若客户迟迟未回复，本期不做催确认功能。 |
| 客户已回复 | 展示客户选择结果、客户原文、客户内容翻译、默认同步人员。 | 继续回复客户、确认同步、转交内部处理、关闭工单、确认完结。 | “确认同步”只同步给提出人和相关处理人，状态变为客户已确认，不直接完结。 |
| 客户已确认 | 展示已同步客户信息和内部待确认结果。 | 关闭工单、确认完结。 | 发起人可确认完结；非发起人只能查看或继续内部处理。 |
| 已完结 | 展示最终处理意见、图片附件和完结时间。 | 只可查看。 | 所有操作按钮隐藏，只保留右上角关闭弹窗。旧数据中的“已关闭”统一迁移展示为“已完结”。 |

**(4) 图片附件区**

| 字段/控件名称 | 功能展现描述 | 业务规则与异常边界处理 |
| --- | --- | --- |
| 实物图 | 展示或上传实物图。业务发送客户时可勾选是否随内容发送。 | 非必填。勾选说明统一为“勾选后随内容发送给客户”。 |
| 链接图 | 展示或上传链接图。业务发送客户时可勾选是否随内容发送。 | 非必填。与实物图使用相同提示文案，上传后支持预览大图和删除。 |
| 图片预览 | 点击缩略图查看大图。 | 大图预览不改变表单内容；删除需二次确认或明显删除入口。 |

**(5) 历史记录区**

| 字段/控件名称 | 功能展现描述 | 业务规则与异常边界处理 |
| --- | --- | --- |
| 全部 | 默认展示工单内容、内部处理、客户沟通三类记录汇总，按时间倒序排列。 | 流转记录不进入“全部”，避免操作日志干扰业务内容。不同类型用标签和轻微背景区分。 |
| 工单内容 | 展示创建时的问题内容、模板标识、实物图、链接图。 | 模板只展示名称，例如“色差问题”；自定义创建展示“自定义内容”。 |
| 内部处理 | 展示处理人提交意见、转交意见、关闭意见及图片。 | 支持从“上一环节意见”的查看详情高亮定位到对应记录。 |
| 客户沟通 | 展示业务发送和客户回复。 | 业务消息靠右绿色，客户消息靠左蓝色；同一条消息不得重复展示。 |
| 历史工单 | 展示当前商品规格在不同订单中的历史工单，并支持按客户ID、订单编号、问题类型筛选。 | 显示订单编号、客户编号、工单模板、创建时间、状态和摘要；筛选仅影响历史工单结果，不影响当前工单详情。工单模板以独立背景标签展示，例如“尺寸问题”，不展示“模板：”前缀；点击“查看”必须打开新的浏览器 Tab 页展示对应订单/工单详情，不覆盖当前工单详情弹窗。 |
| 流转记录 | 单独展示状态变更、处理人变更、转交路径。 | 按时间倒序展示，不进入“全部”聚合。 |

**③ 交互与跳转说明**

- 左侧切换工单时，中间当前处理区、底部操作按钮、右侧历史记录同步刷新。
- 当前处理区固定为处理主区域，员工在任意滚动位置都能明确本次需要填写的内容。
- 点击“查看详情”时，右侧历史记录自动切换到对应分类，并高亮当前关联记录。
- 点击“关闭工单”不是直接关闭，需要填写本次处理意见和图片附件；确认后同步给提出人和对应处理人。
- 点击“确认完结”只有发起人可执行；例如发起人为质检主管李倩，当前登录人为李倩时必须展示确认完结入口。

**④ 关联的隐藏后端模块及解决方案**

| 关联模块 | 影响点 | 解决方案 |
| --- | --- | --- |
| 工单状态机 | 状态需覆盖待处理、待客户确认、客户已回复、客户已确认、已完结。 | 后端统一状态流转，不允许前端自行改状态；提交时校验当前状态版本。 |
| 权限服务 | 业务可发送客户确认，发起人可完结，当前处理人可处理或转交。 | 每个动作后端做角色与关系校验，避免只靠前端隐藏按钮。 |
| 历史记录服务 | 右侧历史记录需要按类型聚合且避免重复。 | 内部处理、客户沟通、流转记录分表或分类型存储；聚合时按唯一记录ID去重。 |
| 同步待办服务 | 提交意见、客户同步、关闭工单都可能生成相关人员待办。 | 同步对象由动作规则计算，不由前端手填；重复同步时合并为最新待办。 |
| 历史工单服务 | 工单详情需要跨订单查看历史工单。 | 根据规格Key查询有权限范围内的历史工单，结果按创建时间倒序展示；支持客户ID、订单编号、问题类型筛选；排除当前工单避免重复。 |

### 2.1 我的待办页

**① 核心业务逻辑介绍**

我的待办用于集中展示当前员工需要处理、本人发起、待客户确认、客户已回复、客户已确认、已完结等工单。页面标题区提供“我的待办 / 全部工单”模式切换：默认打开“我的待办”，仅展示与当前员工相关的工单；切换到“全部工单”后，有权限人员可查看所有订单的工单记录。待办列表只做定位和快速进入，不承载复杂处理表单；点击下一步进入同一个工单详情弹窗。

**② 字段级别功能描述与异常流程**

**(1) 左侧导航与入口**

| 字段/控件名称 | 功能展现描述 | 业务规则与异常边界处理 |
| --- | --- | --- |
| 工作台 | 一级目录增加红色数字气泡。 | 统计当前登录人“待我处理”的工单数量；统计失败时不影响导航展示。 |
| 我的待办 | 默认蓝色文字，选中后保持蓝色高亮并展示红色气泡。 | 气泡统计当前登录人“待我处理”的工单数量；只有我的待办、代购订单、工单模板三个可点击入口使用蓝色标识。 |
| 工单模板 | 位于系统设置下，默认蓝色文字。 | 无模板权限时可隐藏或置灰。 |

**(2) 筛选条件区**

| 字段/控件名称 | 功能展现描述 | 业务规则与异常边界处理 |
| --- | --- | --- |
| 客户ID | 输入客户编号。 | 放在订单/工单筛选前，支持模糊搜索；全部工单模式下也按数据权限过滤。 |
| 订单/工单 | 输入订单编号或工单编号。 | 支持模糊搜索；工单编号按序号搜索时需结合商品或订单范围过滤。 |
| 创建时间 | 一体式时间段筛选控件，展示“开始日期 至 结束日期”。 | 开始日期为空时从最早记录开始；结束日期为空时查询到当前日期；结束日期早于开始日期时提示修正。 |
| 状态 | 下拉筛选全部状态。 | 状态顺序与Tab保持一致。 |
| 处理人 | 下拉筛选当前处理人。 | 默认全部处理人；仅展示有权限查看的人员。 |
| 查询 / 重置 | 查询刷新列表，重置清空筛选。 | 查询失败时保留原列表并提示失败。 |

**(3) 状态Tab与列表字段**

| 字段/控件名称 | 功能展现描述 | 业务规则与异常边界处理 |
| --- | --- | --- |
| 状态Tab | 我的待办模式顺序为：待我处理、我发起的、待处理、待客户确认、客户已回复、客户已确认、已完结、全部；全部工单模式是独立页面视图，Tab 顺序为：待处理、待客户确认、客户已回复、客户已确认、已完结、全部。 | 数量气泡使用红色；默认不自动高亮第一行数据。“全部”按当前页面模式汇总：我的待办模式按本人关系过滤，全部工单模式按权限查看全量历史。 |
| 工单编号 | 展示序号，例如 001。 | 列宽保证不换行。 |
| 创建时间 | 展示工单创建时间。 | 列宽保证不换行；用于辅助判断处理时效与历史先后。 |
| 订单/客户 | 展示订单编号和客户编号，订单编号为可点击链接。 | 点击订单编号跳转到订单详情。 |
| 商品与问题 | 展示商品缩略图、商品名、问题摘要。 | 列表整体高度压缩，图片缩小；长文本单行或两行省略。 |
| 状态 | 展示当前工单状态标签。 | 已关闭旧状态统一展示为已完结。 |
| 当前处理人 | 展示当前处理部门和人名。 | 内容不换行。 |
| 下一步 | 展示去处理、查看、同步客户信息等动作。 | 待客户确认列表按钮文案为“查看”，不再模拟客户回复。 |

**③ 交互与跳转说明**

- 点击“我的待办 / 全部工单”切换页面整体查询范围，切换后保留筛选项但按新范围重新计算Tab数量。
- 点击“去处理”打开工单详情弹窗，并定位到该工单当前处理状态。
- 点击“查看”进入只读或等待状态的工单详情。
- 点击订单编号跳转到订单详情页面。
- 我的待办右上角不展示“回订单详情”“打开当前工单”等额外按钮，避免操作入口重复。

**④ 关联的隐藏后端模块及解决方案**

| 关联模块 | 影响点 | 解决方案 |
| --- | --- | --- |
| 待办查询服务 | 不同Tab对应不同查询条件。 | 后端根据当前登录人、发起人、当前处理人、状态计算结果，避免前端自行拼接复杂规则。 |
| 全部工单查询服务 | “全部工单”模式需要查看所有订单工单。 | 后端按数据权限返回全量工单，非授权用户不得看到无权限订单。 |
| 创建时间索引 | 待办列表需要按创建时间段筛选。 | 工单主表创建时间建立查询索引，避免时间筛选导致列表查询变慢。 |
| 订单跳转服务 | 订单编号可跳转。 | 列表返回订单详情URL或订单ID，由前端统一跳转。 |
| 状态兼容 | 旧状态与新状态命名需要兼容。 | 后端统一映射已关闭为已完结，避免列表和详情不一致。 |

### 2.2 工单模板页

**① 核心业务逻辑介绍**

工单模板用于维护常见商品问题的标准内容，减少创建工单时重复输入。模板支持多语言翻译，但创建工单只回填中文；业务对客发送时可使用客户语言内容作为默认发送内容。

**② 字段级别功能描述与异常流程**

**(1) 模板列表区**

| 字段/控件名称 | 功能展现描述 | 业务规则与异常边界处理 |
| --- | --- | --- |
| 模板名称 | 展示色差问题、尺寸问题等模板名称。 | 名称不可重复；删除前需确认是否有历史工单引用。 |
| 模板内容 | 第一行展示中文模板内容摘要。 | 内容保留换行，列表中超出显示省略。 |
| 状态 / 语言 / 创建时间 | 第二行展示启用状态、7/7语言、创建时间。 | 停用模板不出现在创建工单下拉中，但历史工单仍展示原模板名称。 |
| 操作 | 支持复制、停用、删除。 | 删除仅影响后续选择，不得清空历史工单内容。 |

**(2) 模板编辑区**

| 字段/控件名称 | 功能展现描述 | 业务规则与异常边界处理 |
| --- | --- | --- |
| 中文内容 | 维护模板主内容。 | 必填，是其他语言翻译的基准内容。 |
| 英语 / 日语 / 韩语 / 阿拉伯语 / 西班牙语 / 俄语 | 维护多语言模板内容。 | 可手动编辑；缺失时业务发送客户需提示补充，不自动发送空内容。 |
| 启用状态 | 控制模板是否可选。 | 已被历史工单使用的模板停用后，历史展示不受影响。 |

**③ 交互与跳转说明**

- 新增或编辑模板后，创建工单弹窗的工单模板下拉实时或刷新后可用。
- 删除模板需二次确认。
- 复制模板生成新模板草稿，名称需重新编辑后才能保存。

**④ 关联的隐藏后端模块及解决方案**

| 关联模块 | 影响点 | 解决方案 |
| --- | --- | --- |
| 模板服务 | 模板多语言维护与启停。 | 模板表保存主语言内容与多语言内容，历史工单保存创建时实际使用内容快照。 |
| 对客发送模块 | 业务发送客户时读取对应客户语言模板。 | 有模板语言内容时自动填入；无模板或自定义内容时由业务手动填写。 |

---

## 3. 【用户端】客户确认与对客沟通回传

### 3.1 客户确认页（现有页面承接）

**① 核心业务逻辑介绍**

客户确认页已存在，不属于本次页面重做范围。本期只要求内部工单系统能生成对客确认内容，并接收客户选择结果和客户原文，供业务翻译后同步给提出人与相关处理人。

**② 字段级别功能描述与异常流程**

| 字段/控件名称 | 功能展现描述 | 业务规则与异常边界处理 |
| --- | --- | --- |
| 客户选择结果 | 客户选择可接受、不接受、退货等结构化结果。 | 回传后进入“客户已回复”。客户结果为空时不得进入客户已回复。 |
| 客户原文 | 客户填写的原始语言内容。 | 原文只用于业务翻译和历史记录，不直接作为内部结论。 |
| 客户内容翻译 | 业务将客户原文翻译为中文。 | 确认同步前必填；同步后发送给提出人和相关处理人。 |
| 回复时间 | 客户提交回复的时间。 | 用于客户沟通记录倒序展示。 |

**③ 交互与跳转说明**

- 客户提交回复后，内部工单状态变为客户已回复，业务待办出现“同步客户信息”动作。
- 业务可继续回复客户，形成多轮客户沟通；每次沟通进入客户沟通记录，按时间倒序展示。
- 本期不在客户确认页新增交互，只约束内部系统的发送和回传数据。

**④ 关联的隐藏后端模块及解决方案**

| 关联模块 | 影响点 | 解决方案 |
| --- | --- | --- |
| 客户确认回调 | 客户回复需准确回到对应工单。 | 发送客户确认时生成唯一确认链接或确认ID，回传时按ID定位工单。 |
| 客户沟通记录 | 业务发送和客户回复不得重复展示。 | 每次发送和回传生成唯一沟通记录ID，聚合展示时按ID去重。 |

---

## 4. 其他关联模块 (连带核心后端系统影响与阻击规范)

| 前端表现 / 用户侧操作 | 潜在漏洞与防线断层风险点 | 核心关联的后端微服务及硬性加固要求 |
| --- | --- | --- |
| 订单详情和我的待办都可以打开同一工单详情弹窗。 | 两个入口同时处理同一工单，可能出现状态覆盖或重复提交。 | 工单提交接口必须校验状态版本号；状态已变化时提示刷新，不允许覆盖最新处理结果。 |
| 创建工单只选择一个处理人。 | 如果后续仍保留多人处理旧逻辑，会造成待办重复和责任不清。 | 工单主流程只保存一个当前处理人；协同信息通过转交记录或同步对象记录，不作为当前处理人。 |
| 只有业务可发送客户确认。 | 非业务角色误发客户内容，可能导致翻译错误或客户沟通口径不一致。 | 对客发送接口必须校验业务角色或订单业务负责人身份。 |
| 发起人才能确认完结。 | 处理人提前完结会导致提出人未确认结果。 | 完结接口必须校验发起人身份；非发起人只允许关闭、转交或提交处理意见。 |
| 客户已回复后点击确认同步，不直接完结。 | 客户信息未同步给提出人和处理人就结束工单，内部无法追溯。 | 确认同步生成同步记录和相关人员待办，状态变更为客户已确认。 |
| 关闭工单需要填写本次处理意见和图片附件。 | 直接关闭会丢失关闭依据。 | 关闭接口复用处理意见结构，保存文本、实物图、链接图，并同步给相关人员。 |
| 图片附件支持点击、拖拽、粘贴上传。 | 不同上传入口可能产生重复附件或附件类型丢失。 | 附件服务保存来源类型、附件分组、排序、去重标识和预览地址。 |
| 历史记录按全部、工单内容、内部处理、客户沟通、流转记录查看。 | 全部列表如果混入流转日志会干扰业务阅读，若客户沟通重复会造成误判。 | 聚合接口按记录类型返回；全部只合并工单内容、内部处理、客户沟通，并按唯一ID去重。 |
| 我的待办增加“全部工单”模式。 | 若不做权限控制，普通员工可能看到无权限订单的工单。 | 全部工单查询必须走数据权限服务，只返回当前用户组织、角色、订单权限允许查看的工单。 |
| 我的待办按创建时间段筛选。 | 时间字段未索引会造成列表查询慢，结束日期边界处理错误会漏数据。 | 工单主表创建时间需建立索引；结束日期按当天 23:59:59 计算。 |
| 创建工单和详情可查看历史工单。 | 商品ID不同但规格相同，如果只按商品ID查会漏掉历史问题；如果规格匹配过宽会误召回无关商品。 | 建立规格标准化Key，至少组合商品名称、颜色、尺码、属性；查询结果按权限过滤并按创建时间倒序。 |
| 创建和转交选择处理人时过滤质控角色。 | 质控部门角色过多会导致误派，处理链路变长。 | 质控部门只返回初检、组长、主管三个角色，其余质控角色不进入处理人下拉。 |
| 工单编号只展示 001、002、003。 | 仅靠序号无法全局唯一，开发若误用序号作为主键会产生冲突。 | 展示编号与内部ID分离；所有接口使用内部唯一ID，序号只作为商品维度展示字段。 |
| 工单模板支持多语言。 | 模板停用、删除或语言缺失会影响历史工单和对客发送。 | 历史工单保存内容快照；删除模板不删除历史引用；发送客户前校验目标语言内容是否存在。 |
