# 订单详情页优化审计待确认问题单

生成日期：2026-05-22  
状态：待产品/开发确认  
说明：以下问题不阻塞测试文档设计，但会影响 Agent2 执行范围、预期口径和数据准备。

| 编号 | 问题 | 当前 PRD 信息 | 风险影响 | 建议确认口径 |
|:--|:--|:--|:--|:--|
| Q-001 | 采购差价负数拦截是否进入本次版本 | PRD 标记“暂缓” | 决定采购确认负数拦截用例是否执行 | 若不进入，TC-OD-AMT-001 至 TC-OD-AMT-004 标记 Skip |
| Q-002 | 店铺返现二次确认是否进入本次版本 | PRD 标记“看开发评估” | 决定返现确认链路是否执行 | 若不进入，TC-OD-CB-001 至 TC-OD-CB-005 标记 Skip |
| Q-003 | 店铺返现确认后是否立即同步采购差价 | PRD 写“未确认前不同步”，确认后只说明采购员无权限修改 | 影响金额预期与采购差价断言 | 明确确认后同步公式与生效时机 |
| Q-004 | 店铺返现确认后哪些角色可修改或反确认 | PRD 仅说明采购员无权限再次修改 | 影响权限用例 | 明确主管、财务、管理员是否可改或反确认 |
| Q-005 | 商品 ID 对应哪个 SKU 字段 | PRD 写“商品的 SKU” | 影响精确匹配测试数据准备 | 明确字段名、大小写、空格处理规则 |
| Q-006 | 已入库规则中发货数量不为 0 的商品归属 | PRD 只定义“正品=已入库且发货数量=0”为已入库 | 影响商品状态边界 | 明确发货数量不为 0 时状态归属 |
| Q-007 | 多质检状态同时存在时是否可命中多个筛选 | PRD 按对应数量大于 0 定义 | 影响质检筛选预期 | 建议允许被多个状态筛选命中，但结果不得重复 |
| Q-008 | 未确认筛选的数量公式在多商品订单中按任一商品还是全部商品判断 | PRD 写“当筛选商品符合采购数=不良数+正品数” | 影响订单级筛选结果 | 明确商品级命中后订单展示，还是订单内全商品均满足 |
| Q-009 | 购入数排序是否进入本次版本 | PRD 标记“开发评估” | 决定排序用例是否执行 | 若不进入，TC-OD-LST-007 至 TC-OD-LST-010 标记 Skip |
| Q-010 | 购入数为 0 是否等同为空 | PRD 只写“购入数量为空” | 影响排序边界 | 建议区分 null/空字符串与数值 0 |
| Q-011 | 筛选结果是仅展示命中商品，还是展示店铺并高亮命中商品 | PRD 只要求添加筛选 | 影响详情页展示预期 | 明确筛选后的商品展示策略 |
| Q-012 | 新版 UI 图以哪个版本为验收基准 | PRD 写“后续 UI 会提交新版设计图” | 影响视觉验收 | 明确最终 UI 截止版本 |

## 执行建议

| 范围 | 处理方式 |
|:--|:--|
| 已明确进入版本的需求 | 订单详情页筛选栏、金额是否确认筛选，作为主执行队列 |
| 暂缓/开发评估需求 | 产出测试用例，执行前根据确认结果标记 Execute 或 Skip |
| 口径不明需求 | 用例保留“按产品确认结果校验”，Agent2 执行前需拉取确认结论 |

