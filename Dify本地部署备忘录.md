# 🚀 Dify 本地部署备忘录（重启后专用）

> 恭喜您已经成功安装并配置好了 Docker Desktop！
> 接下来请按照这 4 步在命令行（cmd 或 PowerShell）中拉起 Dify 引擎：

### 第一步：找个目录拉取源码
在你想要存放源码的地方（比如 `D:\Dify_Engine`），执行：
```powershell
git clone https://github.com/langgenius/dify.git
```

### 第二步：进入构建目录
```powershell
cd dify/docker
```

### 第三步：复制环境变量配置
```powershell
copy .env.example .env
```

### 第四步：一键启动集群（会下载几GB数据，请耐心等待）
```powershell
docker-compose up -d
```

---

当所有进度条走完，显示 `Started` 后，打开浏览器访问：
👉 `http://localhost`

如果遇到报错空间不足，请回想我们的约定：去 Docker 右上角的齿轮 (Settings) -> Resources -> Disk ->把 `Disk image location` 挪到 D 盘。

---

### 🌟 极客进阶：日常秒级启停单行指令（合成版）
日常使用时，如果您不想每次敲好几行，可以直接复制下面这两条“一键直达”的单行 PowerShell 神级指令（假设您是装在刚刚的路径下）：

**🟢 一键唤醒启动：**
```powershell
cd dify/docker; docker-compose start
```

**🔴 一键休眠停机（释放内存）：**
```powershell
cd dify/docker; docker-compose stop
```

---

### 🔑 本地部署管理员通行证（防遗忘）
* **登录账号**：`369901314@qq.com`
* **管理密码**：`6620372may`
*(如果在浏览器里碰到由于清缓存退出的情况，使用这套凭证登入即可)*

---

### 🧩 进阶核心：如何将测试代码骨架映射到 Dify 面板中
*(当您的大模型配置完毕后，按此流程搭建极客可视化引擎)*

#### 步骤 1：创建全链路测试中枢（本体）
1. 点击 Dify 顶部导航栏的 **【工作室 (Studio)】**。
2. 点击右上角的 **【创建空白应用 (Create from Blank)】**。
3. **⚠️ 关键点**：应用类型务必选择 **【聊天流 (Chatflow)】**（必须选这个，才能在后续的界面中出现对话框，供我们发号施令）。
4. 应用名称：比如取名为 `全球站测开架构引擎`。

#### 步骤 2：建立最强外接大脑（抗幻觉基础）
1. 点击顶部导航栏的 **【知识库 (Knowledge)】**，选择【创建知识库】。
2. 把咱们本地已经锤炼得非常扎实的 `00_全局测试规约库` 文件夹里的那极具价值的几个 Markdown 规范文档一并上传过去。
3. 等它切分完成（Embedding）后，这个知识库就等于是引擎的“法典”。

#### 步骤 3：对照图纸“画节点连线”
1. 回到刚才创建的【聊天流】画板，您的屏幕上默认会有一个 `[开始(Start)]` 节点。
2. 在 Start 节点里，添加一个文本入参变量，名字叫做 `模块需求名称`（用来接收诸如“黑白名单及采购账号”等指令）。
3. 接下来最简单的一件事，就是对照着您本地的那份核心图纸代码（`kickoff_test_engine.md`），照猫画虎在画布上拉出对应的节点连线：
   - 添加 **代码节点 (Code)** 去写个扫 PRD 存在情况的 Python 脚本。
   - 添加 **LLM 节点** 去让 AI 回收刚才建好的知识库，并做安全审计和输出。
   - 最后串上一条连贯的流水线，直达输出终点！
